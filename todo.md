todo:
- change delete task list to be in primary view only for selected list? makes more sense tbh
- don't forget to clean up old subtasks, tasks, lists, etc. if something gets deleted
- also clearing the list id from the task if the parent list is deleted
